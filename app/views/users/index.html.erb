<div class="container-fluid gedf-wrapper">
  <div class="row">
  <!--- \\\\\\\Left Profile Info-->
    <div class="col-md-3">
      <%=render 'layouts/left_column'%>
    </div>
  <!--- \\\\\\\Left Profile Info-->

  <!--- \\\\\\\Middle Container-->
    <div class="col-md-6 gedf-main">
      <% @users.each do |user| %>
        <% next if user == current_user %>
        <div class="card mb-3 p-3">
          <div class="row">
            <div class="col-md-2 col-sm-2">
              <%= image_tag user_avatar(user, 80), class: "rounded-circle" %>
            </div>
            <div class="col-md-7 col-sm-7">
              <h5><a href="<%=user_path(user)%>" class="profile-link"><%=user.first_name%> <%=user.last_name%></a></h5>
            </div>
            <div class="col-md-3 col-sm-3">
              <% if !current_user.friend?(user) %>
                <%= button_to "Add Friend", friendships_path(friend_id: user.id), class: "btn btn-primary float-right", method: :post %>
              <% end %>
            </div>
          </div>
        </div>
      <% end %>
    </div>
    <!--- \\\\\\\End Middle Container-->

    <!--- \\\\\\\Side Posts Section-->
    <div class="col-md-3">
        <%=render 'layouts/right_column'%>
    </div>
</div>
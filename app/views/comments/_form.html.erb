<%= simple_form_for :comment, {url: post_comments_path} do |f| %>
  <%= f.input :comment, label: false, input_html: {style: 'height:5rem;'} %>
  <%= f.hidden_field :user_id, :value => current_user.id %>
  <%= f.button :submit, 'Post Comment', class: "btn-primary" %>
<% end %>
